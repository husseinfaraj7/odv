# Server configuration
server.port=${PORT:8080}

# Database configuration (H2 for development, PostgreSQL for production)
spring.datasource.url=${DATABASE_URL:jdbc:h2:mem:testdb}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=${DATABASE_PLATFORM:org.hibernate.dialect.H2Dialect}

# H2 Console (only for development)
spring.h2.console.enabled=true

# Email Configuration
admin.email=${ADMIN_EMAIL:info@odvsicilia.it}

# SMTP Configuration
smtp.host=${BREVO_SMTP_SERVER:smtp-relay.brevo.com}
smtp.port=${BREVO_SMTP_PORT:587}
smtp.username=${BREVO_SMTP_USERNAME:96f5ae002@smtp-brevo.com}
smtp.password=${BREVO_API_KEY:jPLbXdI5mYQ4HnEw}

# Brevo Email Configuration
brevo.api.key=${BREVO_API_KEY:JVfMqU97vcRdFLYr}
brevo.sender.email=${BREVO_SENDER_EMAIL:noreply@odvsicilia.it}
brevo.sender.name=${BREVO_SENDER_NAME:ODV Sicilia}
brevo.admin.email=${ADMIN_EMAIL:info@odvsicilia.it}

# Supabase Configuration
supabase.url=${SUPABASE_URL:https://pejuystijjkjxjctieyb.supabase.co}
supabase.anon.key=${SUPABASE_ANON_KEY:}
supabase.service.role.key=${SUPABASE_SERVICE_ROLE_KEY:}

# Application configuration
app.frontend.url=${FRONTEND_URL:https://odvsicilia.it}
